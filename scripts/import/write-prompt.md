あなたはこのリポジトリ（自分専用の知識ベース）の取り込み工程を、無人で担当しています。
ページの器はすでに作られています。あなたの仕事は **本文 HTML を 1 枚書くこと**だけです。

## 書き込んでよいファイル

`content/pages/{{SLUG}}/index.html` — **これ 1 つだけ**。

`meta.json`・`content/labels.json`・`docs/` 配下は**触らないでください**（別の工程が責任を持っています）。
`npm` やその他のコマンドは実行しません。

## 材料

1. `docs/{{MD_FILE}}` — 原文。ここに書かれていない事実を足さないこと
2. `content/pages/{{SLUG}}/index.html` — 雛形。ここに定義済みのテーマトークンと体裁を土台にする
3. `docs/design.md` の「## 7. ページ HTML の規約」— 守るべき規約
4. `content/pages/{{REFERENCE_SLUG}}/index.html` — 既存ページ。**文章の密度と見出しの粒度をこれに合わせる**

## タイトル

`<title>` と `<h1>` は次の文字列を使ってください（meta.json と揃えるため）:

{{TITLE}}

## このノートの種類

**{{NOTE_KIND}}**

種類に応じて構成を変えます。原文の書き方をなぞるのではなく、**後で引くときに速い形**に組み直してください。

- `qa` … 結論（何が原因で、どうすれば直るか）→ 原因が複数なら切り分けの順に並べる → 操作手順
- `howto` … 何ができるようになるか → 前提 → 手順を番号付きで → つまずきやすい点
- `research` … 結論（どれを選ぶか）→ 選択肢の比較（表が効く）→ 判断の根拠 → 保留した選択肢
- `reference` … 引く対象を前に出す（表・定義リスト中心）→ 補足は後ろ。読み物にしない
- `other` … 原文の構造を尊重しつつ、結論にあたる部分を先頭に出す

## やってはいけないレイアウト（実際に壊れた例）

- **`li` や段落を `display: grid` / `flex` にして、番号や記号の列を作らないこと。**
  `li { display: grid; grid-template-columns: 1.7rem 1fr }` は、li の中身がテキストだけなら
  意図通りに出ます。しかし `<strong>` や `<code>` を1つ挟んだ瞬間、**それぞれが独立したグリッド項目**になり、
  3つ目以降が 1.7rem の狭い列に落ちて**1文字ずつ縦に並びます**。
  番号や記号は `position: absolute` で置き、li 本体は `position: relative` + `padding-left` にしてください。
  （この形はビルド時に機械的に弾かれます）

```css
/* 正しい形 */
ol.steps > li { counter-increment: step; position: relative; padding-left: calc(1.7rem + 0.9rem); }
ol.steps > li::before { content: counter(step); position: absolute; left: 0; width: 1.7rem; text-align: center; }
```

- **日本語の本文が入る表を狭く潰さないこと。** `.scroll-x` の内側に入れたうえで、
  `table { min-width: 36rem }` 程度（列が4つ以上なら 46rem 程度）を指定します。
  指定しないと、狭い画面で1列が2〜3文字まで潰れて縦書きのように読めなくなります。
- **長いパスや URL を含むインラインの `code` には `overflow-wrap: anywhere;` を付けること。**
  折り返せないと、その1語のために文書全体が横スクロールします。

## 守ること（保存前に自分で確認）

- 外部ホストへの参照がゼロ（CDN・Web フォント・外部画像・fetch/XHR。CSP で機械的に遮断されるので、書いても表示されません）
- `<!doctype html>` から `</html>` まで完結した文書
- 明色・暗色の両方で読める（色は雛形の `:root` と `@media (prefers-color-scheme: dark)` のトークンを使う。新しい色を足すなら**必ず両方**に定義する）
- 横幅 375px で文書本体が横スクロールしない（広い表やコードは `pre` / `.scroll-x` の内側でスクロールさせる）
- 雛形の「ここから本文を書く。」というプレースホルダの文が残っていない
- 原文の frontmatter（`tags` や `created` など）を本文に転記していない。分類はアプリ側が持ちます

## 書き方

読み返すのは**同じ場面でまた手が止まった数ヶ月後の自分**です。次を優先します。

- **結論を先に。** 最初の段落で「要するに何か」が分かること
- 操作手順は番号付きリスト。UI のラベルやショートカットは `<code>` で囲む
- 分岐や比較がある話は、文章で並べずに表や見出しで分ける
- 図があると理解が早い箇所だけ、インライン SVG で描く（無理に足さない）
- 原文がスクリーンショットを参照していても、画像は取り込めません。**文章で説明に置き換えます**
- 原文にない推測・一般論で分量を増やさない

書き終えたら、何を書いたかを 1 行で報告してください。
